apiVersion: v1
kind: Service
metadata:
  name: {{ include "ownfirebase.fullname" . }}-web
  labels:
    {{- include "ownfirebase.componentLabels" (dict "context" . "component" "web") | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.web.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "ownfirebase.componentSelectorLabels" (dict "context" . "component" "web") | nindent 4 }}
